#pragma once
#include <entityx/entityx.h>
#include <SFML/Main.hpp>
#include <cmath>
/*
enum struct Direction{
    EAST,
    SOUTH,
    WEST,
    NORTH,
    LEN
};
*/

/*
Direction vectorToDirection(sf::Vector2f vector, Direction fallback);

sf::Vector2f directionToVector(Direction dir);

struct BodyComponent : public entityx::Component<BodyComponent>{
  Direction facing;
  inline BodyComponent(){
    //Cheap randomness based off heap allocation
    facing = Direction::SOUTH;
    }
};
*/
